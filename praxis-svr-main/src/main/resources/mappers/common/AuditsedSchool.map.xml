<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.noriental.praxissvr.question.mapper.AuditsedSchoolMapper">

    <select id="queryAuditsedSchoolList" parameterType="long" resultType="long">
        SELECT
        org_id
        FROM entity_auditsed_school
        WHERE visible=1
    </select>


</mapper>

